# AgentCLIExecutor Configuration

# Base URL for the VCP Server (Required for asynchronous callbacks)
# Typically: http://localhost:PORT
CALLBACK_BASE_URL=http://localhost:3000

# Optional: Global timeout override (seconds)
# DEFAULT_TIMEOUT=600

# Optional: API Keys for specific agents if they need to be injected via environment variables
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-...
